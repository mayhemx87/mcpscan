---
name: New client / config path
about: An AI editor or agent auto-loads MCP configs from a path mcpscan doesn't scan
title: 'Support <client name>'
labels: new-client, good first issue
---

**Client** (editor/agent name and link):

**Repo-relative config path(s) it auto-loads** (e.g. `.foo/mcp.json`):

**Link to the client's documentation for that path** (required — we only scan
paths that are actually auto-loaded):

**Config shape** (top-level `mcpServers`, VS Code-style `servers`, or embedded
under a nested key in a settings file? A minimal example helps):

```json
```
