---
name: False positive / false negative
about: A config was flagged that shouldn't be, or a risky config wasn't flagged
title: ''
labels: detection-quality
---

**The config (sanitized — replace any real secrets/hostnames):**

```json
```

**File path in the repo** (e.g. `.cursor/mcp.json`):

**What mcpscan reported** (paste output, ideally `--json`):

**What you expected instead, and why:**

**mcpscan version** (`mcpscan version`):
